# Jekyll configuration for GitHub Pages
# Include the apps-config.yml file in the build
include:
  - apps-config.yml
  - lavender-morning/assets
  
# Explicitly keep assets folders
keep_files:
  - lavender-morning/assets
